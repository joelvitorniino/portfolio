generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Project {
  id          Int    @id @default(autoincrement())
  title       String
  description String
  url         String
  imagelink   String
  repository  String

  @@map("projects")
}
